#if($struts2 == "false")
#if ($parameter.custom)
#set($path = ${parameter.getJsp().getUseCase().getPackagePath()})
#set($package=$path.replace('/','.'))
#set($generatedFile = "${path}/${parameter.name}.jsp")
<!-- Implemente aqui o componente -->
	#if ($requiredness && $parameter.required)<label class="textfieldLabel" for="${parameter.name}"><bean:message key="$parameter.messageKey"/> *</label>#else<label class="textfieldLabel" for="${parameter.name}"><bean:message key="$parameter.messageKey"/></label>#end
	<input type="text" name="${parameter.name}" value="<c:out value=""/>" id="${parameter.styleId}"/>
#end
#end