## SOMENTE GERADO EM PROJETOS MODULARIZADOS
#if($modulo == 'true')
#foreach ($message in $useCase.messages.entrySet())
#if ($message.value)
$message.key=$message.value
#else
$message.key=
#end
#end
##
##
#foreach ($jsp in $useCase.pages)
#foreach($field in $jsp.allUniqueActionParameters)
invalid.fieldvalue.${field.name}=The value must be of type ${field.type.name}
#end
#end
##
##
#end